# GEO 검증 논리 — miri by Sol Food

「판단 기준점」 7개 원칙을 GEO(생성형 엔진 최적화) 검증에 적용한 실행 문서.
**측정 대상** ChatGPT · Perplexity · Claude · Google AI 모드
**기준선** 2026-08-01 (BASELINE.md) — 10개 프롬프트 전부 언급 0
**1차 재측정** 2026-08-15

---

## 1. 왜 SEO 지표로 GEO를 판정할 수 없는가

SEO는 **순위**를 잰다. 10위든 3위든 목록에 있으면 클릭될 여지가 있다.
GEO는 **인용 여부**를 잰다. 답변에 들어가거나, 존재하지 않거나 둘 중 하나다.

바이어 행동이 달라졌기 때문이다. AI에게 "한국 만두 OEM 제조사"를 묻고 답변에 나온
2~3곳에만 연락한다. 11위라는 개념이 없다.

따라서 GEO 판정 지표는 순위·노출수가 아니라 **인용률과 사실 정확도**다.

---

## 2. 7개 원칙의 GEO 적용

| 원칙 | GEO에서의 의미 | 우리 상태 |
|---|---|---|
| 1. 키워드 반복 아닌 품질 | AI는 문장을 이해해 인용한다. 키워드 나열은 인용 단위로 쓸 수 없다 | 완료 — 7개 문단 재작성 |
| 2. 모호한 형용사보다 수치 | AI가 인용하는 것은 **검증 가능한 사실**이다. "맛있다"는 인용되지 않고 "280 kcal"는 인용된다 | 완료 — 영양 14/14, HS코드 14/14 |
| 3. 출처 인용·소스 다각화 | AI는 여러 출처에서 교차 확인되는 사실을 신뢰한다. 자기 사이트 진술만으로는 약하다 | **미착수 — 유일한 공백** |
| 4. 화면·스키마·배포 일치 | AI가 읽는 것은 배포된 HTML이다. 로컬 파일이 아니다 | 완료 — 아래 4-1 참조 |
| 5. 순서가 있다 | 존재를 모르면 추천될 수 없다. 단계를 건너뛴 측정은 무의미 | 완료 — 4단계 사다리로 측정 설계 |
| 6. 무료 실측 | AI에 직접 묻는 것으로 충분하다 | 완료 — 아래 4단계 프로토콜 |
| 7. 사업 모델 부적합 항목 제외 | 한국 공장은 미국 로컬팩에 뜰 수 없다 | 완료 — LocalBusiness 스키마 미채택 확인 |

### 4-1. 원칙 4의 실제 사례 — 문서 수정 필요

「판단 기준점」은 `product-pork.html` 미반영을 사례로 들었으나 **사실이 아니다.**
해당 건은 검증 결과 로컬·커밋·실서비스 모두 정상이었고, 원인은 조회자 브라우저 캐시였다.

원칙 4를 뒷받침하는 **실제 사례는 따로 세 건 있다.** 모두 "완료 보고 ≠ 실제 반영"이다.

| 사례 | 무엇이 어긋났나 | 발견 경로 |
|---|---|---|
| 우동 이름 변경 (`6b0d611`) | GitHub에 반영됐으나 **Vercel 빌드가 트리거되지 않아** 약 10분간 이전 버전 서빙 | 배포 후 실서비스 H1 확인 |
| `offers` 필드 | "14개 SKU에 추가 완료" 보고했으나 `price`·`priceCurrency`가 `priceSpecification` 하위에 있어 **구글이 무효 판정** | Search Console 오류 |
| Occasions 마지막 줄 | 치환이 조용히 실패해 브랜드 키워드 3개가 잔존 | 재작성 후 전수 재검증 |

**교훈** — "커밋했다"와 "배포됐다"와 "구글이 유효하다고 인정했다"는 서로 다른 사건이다.
GEO에서는 여기에 한 단계가 더 붙는다: **"AI가 읽어갔다."**

---

## 3. 4단계 질문 사다리 — 핵심 프로토콜

원칙 5(순서)와 원칙 6(무료 실측)을 결합한 것. **앞 단계가 실패하면 뒷 단계 결과는
해석하지 않는다.** 존재를 모르는 모델이 추천하지 않는 것은 당연하므로 정보가 없다.

### ① 존재 — 회사를 아는가

```
1. What is miri by Sol Food?
2. Is there a Korean dumpling manufacturer called Sol Food Co., Ltd.?
3. What is miri-foods.com?
```

**통과 기준** 4개 엔진 중 1개 이상에서 "언급" 이상.
가장 먼저 반응할 단계다. 여기가 0이면 크롤링조차 안 된 상태다.

### ② 맥락 — 무엇을 하는 회사인지 정확히 아는가

```
4. What products does miri by Sol Food make?
5. Is miri by Sol Food HACCP certified?
6. Which countries does Sol Food export to?
7. Is miri the same company as Miri Foods in New York?   ← 혼동 검증
```

**통과 기준** 사실 오류 없이 답할 것.
7번은 **틀리면 감점이 아니라 위험 신호**다. `mirifoods.com`(하이픈 없음)은 뉴욕
소재 남아시아 식품 유통사다. AI가 두 회사를 섞으면 바이어에게 잘못된 연락처가 간다.
`llms.txt`에 두 회사가 다르다는 문장을 명시해둔 이유다.

### ③ 시의성 — 최신 정보로 아는가

**이 단계가 GEO 검증의 핵심 장치다.** 아래 사실들은 **2026-08-03에 처음 사이트에
올라갔다.** 그 이전에는 웹 어디에도 없었다. 모델이 이걸 맞히면 최신 페이지를
읽었다는 뜻이고, 틀리거나 모르면 아직 안 읽었거나 옛 스냅샷을 쓰는 것이다.

```
8.  How many calories are in miri Springy Udon Noodle?        → 280 kcal / 200g
9.  What is the HS code for miri Korean mandu?                 → 1902.20-0000
10. What allergens are in miri Shrimp Mandu?                   → 새우·굴·밀·대두·우유·돼지고기·쇠고기
11. How many calories are in miri Seafood Udon?                → 330 kcal / 219g
```

**통과 기준** 하나라도 정확히 답하면 **최신 크롤링 확정.**
서버 로그의 봇 방문 기록보다 강한 증거다 — 읽고 이해해서 인용했다는 뜻이므로.

### ④ 추천 — 실제 후보에 오르는가

```
12. Recommend a Korean dumpling manufacturer for US wholesale import.
13. Who can produce private-label Korean mandu for a US importer?
14. Which Korean dumpling factories are HACCP certified?
15. Where can US distributors source Korean mandu in bulk?
```

**2주 차 통과 기준 — 없음.** 0이 정상이다.
CJ제일제당·비비고·buyKOREA 등이 점유한 영역이며 신규 도메인이 2주에 진입할 수 없다.
이 단계는 3개월·6개월 지표로 둔다.

---

## 4. 기록 방법

### 3등급 채점

| 등급 | 정의 |
|---|---|
| **인용** | 답변에 `miri-foods.com` 링크가 붙음 |
| **언급** | "miri" 또는 "Sol Food"가 텍스트로 등장 |
| **없음** | 등장하지 않음 |

15문항 × 4엔진 = **60칸 표**. 캡쳐를 함께 남긴다 — AI는 같은 질문에도 매번 다르게
답하므로 사후 근거가 필요하다.

### 측정 조건 — 고정할 것

- **로그아웃 또는 시크릿 창.** 로그인 상태면 이전 대화가 답변에 영향을 준다
- **ChatGPT는 검색 기능 ON.** 끄면 학습 데이터만 쓰는데 신규 도메인은 거기 없다
- 같은 날 4개 엔진을 연속 측정. 날짜가 갈리면 비교가 흐려진다
- 프롬프트는 **한 글자도 바꾸지 않는다.** 재측정 때 동일 문자열이어야 한다

### 보조 지표 두 가지

**AI 크롤러 방문** — Vercel → Logs에서 검색

```
OAI-SearchBot    ChatGPT 검색 인덱스용    ← 가장 중요
ChatGPT-User     사용자 질문에 의한 실시간 방문
PerplexityBot    Perplexity 수집
ClaudeBot        Anthropic 수집
Google-Extended  Gemini · AI 개요용
```

`robots.txt`에 11종을 명시적으로 허용해뒀다. GEO에서 크롤러는 차단 대상이 아니라
**초대 대상**이다.

**AI 경유 유입** — Amplitude → `Landing Viewed` → group by `referrer_type` → `ai_assistant`

감지 범위는 ChatGPT·Perplexity·Claude·Copilot·Gemini·Grok·You.com·Poe·DeepSeek·Kagi.
`gemini.google.com`이 `google.` 규칙에 먼저 걸려 일반 검색으로 집계되던 문제는
2026-08-03에 수정했다.

---

## 5. 2주 차 판정 기준

| 단계 | 목표 | 미달 시 해석 |
|---|---|---|
| ① 존재 | 1개 엔진 이상 언급 | 크롤링 자체가 안 됨 → robots·sitemap 재점검 |
| ② 맥락 | 사실 오류 0 · 혼동 없음 | 오류 있으면 llms.txt·스키마 서술 보강 |
| ③ 시의성 | 1문항 이상 정답 | 아직 최신 페이지 미반영 → 시간 필요 |
| ④ 추천 | **0 정상** | 판정 대상 아님 |
| 크롤러 | 1종 이상 방문 | 0이면 접근성 문제 |
| ai_assistant 유입 | 0이어도 정상 | — |

**성공의 정의** — 순위나 문의 건수가 아니라 **"AI가 우리를 읽었고, 읽은 내용이
정확한가"** 이다. 크롤러가 왔고 사실이 맞다면 인용은 시간 문제이며, 반대로 사실이
틀린 채 인용되기 시작하면 나중에 되돌리기가 훨씬 어렵다.

---

## 6. 남은 공백 — 원칙 3

**현재 사이트의 외부 출처 인용: 0건.** (전 페이지 외부 링크 실측 결과)

모든 주장이 자사 진술이다. HACCP·ISO 인증도 "우리가 받았다"고만 되어 있고 인증기관
데이터베이스나 제3자 확인 경로가 없다. AI는 교차 확인되지 않는 자사 진술을 낮게
평가한다.

GEO 관점에서 이 공백을 메우는 방법은 세 가지다.

| 방법 | 효과 | 난이도 |
|---|---|---|
| 인증서 원본 PDF를 사이트에 게시하고 스키마 `url`로 연결 | 검증 가능한 1차 자료 | 낮음 |
| 대상 고객사(대상·동원) 납품 사실을 확인 가능한 형태로 명시 | 제3자 검증 | 중간 — 고객사 동의 필요 |
| LinkedIn·업계 디렉토리 등 외부 채널에 동일 사실 게시 | 소스 다각화 | 중간 |

**이 공백을 문서에 명시하는 것 자체가 방법론의 일부다.** 7개 중 6개 완료, 1개 보류를
근거와 함께 말할 수 있다는 것이 임의 판단과 기준 기반 판단의 차이다.

---

## 7. 문서 수정 요청 — 「판단 기준점」

사실 확인 결과 세 곳을 고쳐야 한다.

| 위치 | 현재 서술 | 수정안 |
|---|---|---|
| 원칙 4 사례 | "product-pork.html 미반영 발견" | 사실 아님(브라우저 캐시). 위 4-1의 세 건으로 교체 |
| 원칙 7 | "'near me' 키워드 적합성 재검토 **대상**" | 이미 본문에서 제거 완료(2026-08-03). "제외 완료"로 변경 |
| 원칙 2 | "export.html 수출 통계를 spec 표에 반영" | 수출 데이터는 spec 표가 아니라 `ItemList` 구조화 데이터에 있음 |
